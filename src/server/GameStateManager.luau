--[[
    GameStateManager.luau (stub)
    ============================
    Phase C: Authoritative server state management.
    Runs fixed-rate tick loop (Heartbeat), advances bike positions,
    packages state snapshots, broadcasts via StateUpdate (UnreliableRemoteEvent).
]]

local GameStateManager = {}

function GameStateManager.init()
    -- Phase C implementation goes here
end

return GameStateManager
